# Streamlit for the sample app
streamlit==1.25.0

# Pytest for testing
pytest==7.4.0

# Optional: for handling environment variables more easily (like secrets)
python-dotenv==1.1.1

# install pillow for streamlit deployment
pillow>=10.0.0